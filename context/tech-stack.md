# Tech Stack

The locked stack lives in **[CLAUDE.md § Tech Stack (Locked)](../CLAUDE.md#tech-stack-locked)** — that is the canonical source. This file exists only as a pointer to prevent drift.

If you need to propose a stack change, edit CLAUDE.md and log a decision in [docs/decisions.md](../docs/decisions.md). Do not duplicate stack rules here.

## Tool Creep Rule

Before adding any new tool: **"How would we solve this with the existing stack?"** That question gets asked first, every time. Most "we need a new tool" conversations end with "we already have it."
